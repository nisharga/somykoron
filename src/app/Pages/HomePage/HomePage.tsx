import { Adds, Contact, HeroSlider, User } from "../../Components";

const HomePage = () => {
  return (
    <div>
      <HeroSlider />
      <User />
      <Adds />
      <Contact />
    </div>
  );
};

export default HomePage;
